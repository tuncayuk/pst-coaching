{
  "id": "library.content_detail",
  "title": "Library Detail",
  "type": "stack",
  "route": "/library/:type/:id",
  "purpose": "Show library item details and progress.",
  "entryPoints": [
    "library.overview",
    "library.downloads"
  ],
  "inputs": [
    {
      "name": "type",
      "type": "string",
      "required": true,
      "source": "route"
    },
    {
      "name": "id",
      "type": "string",
      "required": true,
      "source": "route"
    }
  ],
  "dataDependencies": [
    "ContentItem",
    "UserProgress",
    "Entitlement"
  ],
  "uiStates": {
    "loading": {
      "description": "Show skeletons and prevent destructive actions.",
      "actions": [
        "retry_fetch_if_timeout"
      ]
    },
    "ready": {
      "description": "Primary content is visible and interactive."
    },
    "empty": {
      "description": "No data available; show guidance and a primary CTA."
    },
    "error": {
      "description": "Display error message with retry and support options."
    },
    "offline": {
      "description": "Show offline banner; use cached content when possible."
    }
  },
  "analytics": {
    "screen_viewed": "library_content_detail_viewed",
    "events": []
  },
  "acRefs": [
    "requirements:Favorites and archive"
  ],
  "gates": [
    "subscription_active_or_trial"
  ]
}
