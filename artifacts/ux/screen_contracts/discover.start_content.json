{
  "id": "discover.start_content",
  "title": "Start Content",
  "type": "sheet",
  "route": "/discover/:type/:id/start",
  "purpose": "Capture daily goal and reminder preferences before starting content.",
  "entryPoints": [
    "discover.content_detail"
  ],
  "inputs": [
    {
      "name": "type",
      "type": "string",
      "required": true,
      "source": "route"
    },
    {
      "name": "id",
      "type": "string",
      "required": true,
      "source": "route"
    }
  ],
  "dataDependencies": [
    "UserProgress",
    "Reminder",
    "ContentItem"
  ],
  "uiStates": {
    "loading": {
      "description": "Show skeletons and prevent destructive actions.",
      "actions": [
        "retry_fetch_if_timeout"
      ]
    },
    "ready": {
      "description": "Primary content is visible and interactive."
    },
    "empty": {
      "description": "No data available; show guidance and a primary CTA."
    },
    "error": {
      "description": "Display error message with retry and support options."
    },
    "offline": {
      "description": "Show offline banner; use cached content when possible."
    }
  },
  "analytics": {
    "screen_viewed": "discover_start_content_viewed",
    "events": [
      "daily_goal_set",
      "reminder_set"
    ]
  },
  "acRefs": [
    "requirements:Discovery and catalog"
  ],
  "gates": [
    "subscription_active_or_trial"
  ]
}
